import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  headerContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    margin: 10
  },
  image: {
    height: 50,
    width: 50,
    borderRadius: 50,
    marginRight: 10
  }
});
